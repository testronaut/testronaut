import { it } from 'vitest';

export function applyFileSystemContract() {
  it.todo('writes file');

  it.todo('writes file and creates parent directories recursively');

  it.todo('throws if file exists');

  it.todo('overwrites if file exists and overwrite is true');
}
